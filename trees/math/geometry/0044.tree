\title{切向量与切空间}
\date{2026-04-14}
\taxon{Definition}
\author{author}

\p{设 #{M} 是光滑流形，#{p\in M} 在局部坐标 #{x=(x^1,\ldots,x^d)} 中表示为 #{x_0}。#{p} 处的切向量可写作}

##{V=v^i\frac{\partial}{\partial x^i}.}

\p{它作为对局部函数 #{\phi} 求导的算子而作用：}

##{V(\phi)(x_0)=v^i\left.\frac{\partial\phi}{\partial x^i}\right|_{x=x_0}.}

\p{这里使用 [Einstein 求和约定](0042)。记号 #{\frac{\partial}{\partial x^i}} 中的指标 #{i} 视为下指标，因为它出现在分母中。}

\p{所有 #{p} 处切向量组成向量空间，称为 #{M} 在 #{p} 处的\strong{切空间}，记作 #{T_pM}。在坐标 #{x} 下，坐标微分算子}

##{\frac{\partial}{\partial x^1},\ldots,\frac{\partial}{\partial x^d}}

\p{给出 #{T_pM} 的一组基。切空间本身不依赖坐标，但这组基和切向量的分量依赖坐标。}

\p{在计算中常把点 #{p} 与它在某组局部坐标中的表示 #{x_0} 直接识别；只要记住前者是几何对象、后者是坐标表示，这种省略通常不会造成歧义。}
