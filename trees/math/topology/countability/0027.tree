\title{sigma-紧空间}
\date{2026-03-31}
\taxon{Definition}
\author{author}

\def\X{#{
\mathcal{X}}}

\p{sigma-紧性是“由可数个紧部分拼成整个空间”的性质，常见于分析、测度论和局部紧空间理论中。}

\p{拓扑空间 #{\mathcal{X}} 称为\strong{sigma-紧空间}，如果存在一列紧子空间 #{K_1, K_2, \ldots}，使得
#{\mathcal{X} = \bigcup_{n=1}^{\infty} K_n}。}

\strong{基本例子}

\ul{
  \li{实数直线 #{\mathbb{R}} 是 sigma-紧的，因为
#{\mathbb{R} = \bigcup_{n=1}^{\infty} [-n,n]}，而每个闭区间都紧}
  \li{欧几里得空间 #{\mathbb{R}^n} 也是 sigma-紧的}
  \li{许多局部紧且“大小可控”的空间都自然满足 sigma-紧性}
}

\strong{与其他可数性质的关系}

\ul{
  \li{每个 sigma-紧空间都是 [Lindelof 空间](0026)}
  \li{反过来，Lindelof 空间未必 sigma-紧}
  \li{sigma-紧性强调的是“由紧块可数拼接”，而不是由开集覆盖可数压缩}
}

\strong{为什么重要}

\ul{
  \li{sigma-紧性允许把全局问题拆成一列紧子空间上的局部问题}
  \li{紧子空间通常更容易应用极值、有限子覆盖和紧致性工具}
  \li{因此 sigma-紧空间常是“非紧但仍可控”的理想对象}
}

\p{[返回可数公理](0021)}
