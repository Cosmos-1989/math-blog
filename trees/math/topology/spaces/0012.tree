\title{例子 1.4：序拓扑}
\date{2026-03-24}
\taxon{Example}
\author{author}

\p{对任何全序集合 X，可以定义\strong{序拓扑}（order topology）：}

\p{基开集为：}
\ul{
  \li{开区间 #{(a,b) = \{x \in X \mid a < x < b\}}}
  \li{形如 #{(a,\infty)} 的区间}
  \li{形如 #{(-\infty,b)} 的区间}
}

\p{例子：#{\mathbb{R}} 是全序集，#{\mathbb{R}} 上的序拓扑与通常的度量拓扑一致。}

\p{这说明序拓扑是度量拓扑的推广。}
