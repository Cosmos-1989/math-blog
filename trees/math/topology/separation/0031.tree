\title{Urysohn 空间（T2½）}
\date{2026-03-31}
\taxon{Definition}
\author{author}

\p{Urysohn 性介于 Hausdorff 与正则之间。它要求两点不仅能由不交邻域分离，还能由不交的闭邻域分离。}

\p{拓扑空间 #{\mathcal{X}} 称为\strong{Urysohn 空间}或\strong{T2½ 空间}，如果对任意不同点 #{x \neq y}，存在开集 #{U,V}，使得 #{x \in U}、#{y \in V} 且 #{\overline{U} \cap \overline{V} = \emptyset}。}

\strong{基本关系}

\ul{
  \li{每个 Urysohn 空间都是 [Hausdorff 空间](002B)}
  \li{每个 [正则空间](002C) 也都是 Urysohn 空间}
  \li{因此 Urysohn 性可以看成 Hausdorff 与正则之间的一步过渡}
}

\strong{意义}

\p{Urysohn 性说明两点之间不仅可以暂时分开，而且这种分离在取闭包后仍然保持。它在研究连续函数分离和更强分离公理时很自然地出现。}

\p{[返回分离公理](0028)}
