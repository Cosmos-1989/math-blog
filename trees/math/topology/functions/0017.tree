\title{例子 1.9：Sierpiński 两点空间}
\date{2026-03-24}
\taxon{Example}
\author{author}

\p{集合 #{S = \{0,1\}} 配备拓扑 #{\{\emptyset,\{1\},S\}} 有时称为\strong{Sierpiński 两点空间}。}

\p{\strong{特征函数}：对任意拓扑空间 X 和开集 #{U \subseteq X}，特征函数 #{\chi_U : X \rightarrow S} 定义为 #{\chi_U(x) = 1} 如果 #{x \in U}，否则 #{\chi_U(x) = 0}。}

\p{在 Sierpiński 拓扑中，这个特征函数是连续的。}

\p{\strong{重要结论}：每个连续函数 #{f : X -> S} 都具有 #{f = \chi_U} 的形式，其中 #{U = f^{-1}(\{1\})}。因此，X 的开子集与连续函数 #{X \rightarrow S} 一一对应。}

\p{换句话说，#{\mathrm{Top}(X,S)} 与 X 的拓扑精确对应。这体现了范畴论的一个重要哲学：对象由其与其他对象的关系决定。}
