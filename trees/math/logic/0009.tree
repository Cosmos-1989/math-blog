\title{正交模格：量子逻辑的核心结构}
\date{2025-03-09}
\taxon{Definition}
\author{author}

\p{正交模格是量子逻辑的代数基础。虽然量子逻辑不满足分配律，但它满足一个较弱的条件——正交模律。}

\strong{正交模律}

\p{一个正交格 #{L = (L, \leq, ()', O, I)} 称为\strong{正交模格}（orthomodular lattice），如果满足正交模律：}

##{a \wedge (a' \vee (a \wedge b)) \leq b, \quad \text{对所有 } a, b \in L}

\p{这个定律可以直观地理解为：如果 #{a} 成立，那么在 #{a} 的框架下考虑 #{a' \vee (a \wedge b)} 就等价于考虑 #{b}。}

\strong{等价的刻画}

\p{Mittelstaedt (1978) 证明了在正交格中，以下条件等价：}

\ol{
  \li{正交模律：#{a \wedge (a' \vee (a \wedge b)) \leq b}}
  \li{若 #{a \leq b}，则 #{a = b \wedge (a \vee b')}}
  \li{若 #{a \leq b}，则 #{b = a \vee (a' \wedge b)}}
  \li{若 #{a \leq b} 且 #{c \leq b'}，则 #{b \wedge (a \vee c) = (b \wedge a) \vee (b \wedge c)}}
}

\strong{关键例子}

\ol{
  \li{\strong{布尔代数}：每个布尔代数都是正交模格。这说明正交模格是布尔代数的推广。}

  \li{\strong{希尔伯特空间的投影格}：设 #{\mathcal{H}} 是复希尔伯特空间，#{\mathcal{L}(\mathcal{H})} 是 #{\mathcal{H}} 的闭子空间（或投影算子）的集合，则 #{\mathcal{L}(\mathcal{H})} 构成正交模格。这是量子力学的标准数学框架。}

  \li{\strong{量子Kripke框架的双正交闭集}：对于任意量子Kripke框架 #{F}，其双正交闭集 #{(\mathcal{L}_F, \subseteq, \sim(\cdot), \emptyset, \Sigma)} 构成正交模格。}
}

\strong{非分配性的证明}

\p{存在一个量子Kripke框架 #{F} 使得 #{(\mathcal{L}_F, \subseteq)} 不是分配格。考虑以下框架：}

\p{设 #{\Sigma = \{1, 2, 3, 4\}}，连通关系如图所示。此时：}

##{\mathcal{L}_F = \{\emptyset, \{1\}, \{2\}, \{3\}, \{4\}, \Sigma\}}

\p{可以验证：#{(\{1\} \cap \{2\}) \sqcup \{3\} = \{3\} \neq \Sigma = (\{1\} \sqcup \{3\}) \cap (\{2\} \sqcup \{3\})}，其中 #{\sqcup} 是正交并。}

\strong{正交模格的封闭平行六面体刻画}

\p{正交模格与封闭平行六面体之间存在深刻联系。在三维欧几里得空间 #{\mathbb{R}^3} 中，考虑由一组线性无关向量张成的封闭平行六面体。}

\p{设 #{\vec{v}_1, \vec{v}_2, \vec{v}_3} 是 #{\mathbb{R}^3} 中线性无关的向量，定义其张成的\strong{平行六面体}为：}

##{P = \{a\vec{v}_1 + b\vec{v}_2 + c\vec{v}_3 \mid 0 \leq a, b, c \leq 1\}}

\p{所有这种形式的封闭平行六面体构成的集合，在适当的偏序关系下，构成一个正交模格。}

\strong{与Kochen-Specker定理的联系}

\p{封闭平行六面体的不可数性直接关联Kochen-Specker定理。在 #{\mathbb{R}^3} 中：}

\ol{
  \li{存在\strong{不可数个}封闭平行六面体}
  \li{每个平行六面体对应一个命题（可测量性质）}
  \li{Kochen-Specker定理表明无法为这些命题分配确定的值}
}

\p{这说明量子力学中的可观测量集合具有比经典集合论更为复杂的结构。}

\strong{经典逻辑与量子逻辑的根本区别}

\p{经典逻辑对应\strong{集合论}，其代数结构是布尔代数：}

\ul{
  \li{命题对应集合}
  \li{合取对应交集}
  \li{析取对应并集}
  \li{否定对应补集}
  \li{分配律成立：#{A \cap (B \cup C) = (A \cap B) \cup (A \cap C)}}
}

\p{量子逻辑对应\strong{希尔伯特空间理论}，其代数结构是正交模格：}

\ul{
  \li{命题对应闭子空间}
  \li{合取对应交集}
  \li{析取对应闭包后的直和}
  \li{否定对应正交补}
  \li{分配律\strong{不}成立}
}

\strong{正交模律的物理诠释}

\p{正交模律 #{b = a \vee (a' \wedge b)}（当 #{a \leq b} 时）可以理解为：}

\p{如果 #{a} 成立，那么 #{b} 要么由 #{a} 直接推出，要么由 #{a} 的正交补与 #{b} 的交推出。这反映了量子测量中"已知 #{a} 后对 #{b} 的重新评估"过程。}

\strong{一个著名的开放问题}

\p{能否将每个正交模格嵌入到一个完备的正交模格中？这是量子逻辑领域的一个重要开放问题。}
