\title{量子逻辑的玩具模型：空间直线}
\date{2025-03-09}
\taxon{Example}
\author{author}

\p{为了直观理解量子逻辑，我们考虑一个简单但深刻的玩具模型：三维欧几里得空间中通过固定点的所有直线。}

\strong{基本设定}

\p{固定三维欧几里得空间 #{\mathbb{E}^3} 中的一点 #{O}。定义：}

\ul{
  \li{#{L}：通过 #{O} 的所有直线的集合}
  \li{#{\not\perp}：#{L} 上的二元\strong{非垂直关系}}
}

\p{对于 #{s, t \in L}，#{s \not\perp t} 当且仅当 #{s} 与 #{t} 不垂直。}

\strong{正交补}

\p{对于任意 #{P \subseteq L}，定义其\strong{正交补}：}

##{\sim P = \{s \in L \mid s \perp u \text{ 对所有 } u \in P\}}

\p{直观上，#{\sim P} 包含所有与 #{P} 中每条直线都垂直的直线。}

\strong{例子}

\ol{
  \li{若 #{P = \emptyset}，则 #{\sim P = L}（空集的正交补是全集）}
  \li{若 #{P} 只包含一条直线 #{s}，则 #{\sim P} 是与 #{s} 垂直的平面上的所有直线}
  \li{若 #{P} 包含两条不同直线且确定一个平面 #{Q}，则 #{\sim P} 只包含垂直于 #{Q} 的那条直线}
  \li{若 #{P} 包含三条不在同一平面的直线，则 #{\sim P = \emptyset}}
}

\strong{双正交闭集}

\p{子集 #{P \subseteq L} 称为\strong{双正交闭集}，如果 #{P = \sim\sim P}。}

\p{在这个例子中，双正交闭集恰好有四种：}

\ol{
  \li{空集 #{\emptyset}}
  \li{单元素集（单个直线）}
  \li{通过 #{O} 的平面（包含该平面上所有直线）}
  \li{全集 #{L}}
}

\p{这些双正交闭集构成了量子逻辑的基本结构，它们形成了一个正交模格。}
