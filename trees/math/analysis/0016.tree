\title{例子 1.8：序列空间 l^p}
\date{2026-03-24}
\taxon{Example}
\author{author}

\p{我们可以将赋范空间从 #{\mathbb{R}^n} 推广到无穷维序列空间。}

\p{\strong{l^p 空间}：满足 #{ \sum |x_n|^p < \infty } 的实数序列 #{\{x_n\}} 的集合，记作 #{\ell^p}。配备范数 #{ \|\{x_i\}\|_p = (\sum |x_i|^p)^{1/p} }。}

\p{这是一个完备的赋范空间（巴拿赫空间）。}

\p{\strong{重要现象}：}
\ul{
  \li{不同 p 的 #{\ell^p} 空间底层集合不同}
  \li{例如，序列 #{\{1/n\}} 在 #{\ell^2} 中但不在 #{\ell^1} 中}
  \li{尽管如此，作为拓扑空间，所有 #{\ell^p} 是同胚的（Kadets, 1967）}
}

\p{\strong{l^∞ 空间}：有界序列的空间 #{ \|\{x_i\}\|_\infty = \sup |x_i| }。它与 #{\ell^p}（#{p < \infty}）不同胚。}

\p{注意：#{\mathbb{R}^{\mathbb{N}}} 可以配备积拓扑，这与将 #{\ell^p} 视为赋范空间得到的拓扑完全不同。}
