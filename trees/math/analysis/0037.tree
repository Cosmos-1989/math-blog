\title{拓扑向量空间}
\date{2026-04-09}
\taxon{Definition}
\author{author}

\p{拓扑向量空间是把线性结构与拓扑结构结合起来的基本对象。它要求向量加法与数乘都与拓扑相容，因此是泛函分析最核心的背景之一。}

\p{设 #{E} 是域 #{\mathbb{K}} 上的向量空间，其中 #{\mathbb{K} \in \{\mathbb{R},\mathbb{C}\}}。若 #{E} 上给定一个拓扑 #{\tau}，并且加法映射
#{a : E \times E \to E,\quad (x,y) \mapsto x+y}
和数乘映射
#{m : \mathbb{K} \times E \to E,\quad (\lambda,x) \mapsto \lambda x}
都连续，则称 #{(E,\tau)} 为一个\strong{拓扑向量空间}。}

\p{我们把 #{0 \in E} 的邻域族记作 #{\mathcal{U}_0(E)}，简称\strong{零点邻域系}。在拓扑向量空间中，整个拓扑实际上由零点附近的局部结构决定。}

\strong{基本性质}

\ul{
  \li{对每个 #{x \in E}，平移映射 #{a_x : E \to E,\ y \mapsto x+y} 是同胚}
  \li{对每个 #{\lambda \in \mathbb{K}\setminus\{0\}}，伸缩映射 #{m_\lambda : E \to E,\ x \mapsto \lambda x} 是同胚}
  \li{因此每一点的邻域结构都可由零点邻域经平移得到，拓扑完全由 #{\mathcal{U}_0(E)} 决定}
  \li{对每个零点邻域 #{U \in \mathcal{U}_0(E)}，总存在零点邻域 #{V} 使得 #{V+V \subseteq U}}
}

\strong{进一步理解}

\ul{
  \li{平移不改变拓扑结构，所以拓扑向量空间天然是“齐性”的}
  \li{零点邻域往往还具有吸收性：对任意 #{x \in E}，可通过足够大的伸缩把 #{x} 拉进某个给定的零点邻域}
  \li{这些性质是局部凸空间、赋范空间和 Banach 空间理论的起点}
}

\strong{例子}

\ul{
  \li{每个 [赋范向量空间](0015) 都是拓扑向量空间}
  \li{更一般地，由半范数族生成的拓扑通常给出拓扑向量空间；见 [半范数与局部凸拓扑](0038)}
}

\p{从一般拓扑看，拓扑向量空间是在代数结构上施加连续性条件；从分析角度看，它为极限、连续性和线性运算之间建立了统一语言。}

\p{[返回分析入口](001D)}
