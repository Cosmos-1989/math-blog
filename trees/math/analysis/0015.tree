\title{例子 1.7：赋范向量空间}
\date{2026-03-24}
\taxon{Example}
\author{author}

\p{实数（或复数）向量空间 V 上的范数是一个函数 #{norm : V -> R}，满足：}

\ol{
  \li{正定性：#{norm(v) >= 0}，等号成立当且仅当 #{v = 0}}
  \li{三角不等式：#{norm(v + w) <= norm(v) + norm(w)}}
  \li{齐次性：#{norm(alpha v) = |alpha| norm(v)}}
}

\p{每个赋范向量空间都是度量空间，度量定义为 #{d(x,y) = norm(x-y)}，因此自然成为拓扑空间。}

\p{R^n 上的标准范数：#{norm(x_1,...,x_n) = sqrt(|x_1|^2 + ... + |x_n|^2)}。更一般的 p-范数：#{norm(x_1,...,x_n)_p = (|x_1|^p + ... + |x_n|^p)^{1/p}}（#{p >= 1}）。上确界范数：#{norm(x_1,...,x_n)_inf = max(|x_1|,...,|x_n|)}。}

\p{关键事实：对 R^n 上的任何范数，诱导的拓扑都相同。}
