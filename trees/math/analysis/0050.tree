\title{von Neumann 代数与两个典型例子}
\date{2026-04-24}
\taxon{Example}
\author{author}

\p{由 [von Neumann 双交换子定理](004X)，一个 #{B(H)} 中的含幺 #{*}-子代数 #{M} 是 von Neumann 代数，当且仅当 #{M=M''}；等价地，当且仅当 #{M} 在 #{\mathrm{SOT}} 或 #{\mathrm{WOT}} 下闭。}

\p{双交换子还有一个最小性性质：对任意含幺 #{*}-子代数 #{A\subseteq B(H)}，#{A''} 是包含 #{A} 的最小 von Neumann 代数。因为若 #{A\subseteq M} 且 #{M=M''}，则由 [交换子与双交换子](004V) 的反单调性有 #{M'\subseteq A'}，从而}

##{A''\subseteq M''=M.}

\p{两个典型例子如下。}

\p{\strong{对角算子代数。} 在 #{\ell^2(\mathbb N)} 上考虑}

##{D:=\{\operatorname{diag}(a_1,a_2,\ldots):(a_n)\in \ell^\infty\}.}

\p{若算子 #{T=(t_{ij})} 与每个坐标投影都对易，则非对角元必须为零，所以 #{D'=D}，进而 #{D''=D}。因此 #{D} 是 von Neumann 代数。}

\p{\strong{乘法算子代数。} 在 #{L^2(X,\mu)} 上，对 #{f\in L^\infty(X,\mu)} 定义乘法算子 #{M_f\psi=f\psi}。集合}

##{A:=\{M_f:f\in L^\infty(X,\mu)\}}

\p{满足 #{A'=A}，故 #{A''=A}。这给出交换 von Neumann 代数的原型。}
