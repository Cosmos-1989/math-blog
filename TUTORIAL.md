# Forester 教程：如何创建数学博客页面

本教程将指导你如何在 Forester 网站中添加新页面、编写内容，以及创建页面间链接。

---

## 目录

1. [创建新页面](#1-创建新页面)
2. [页面基本结构](#2-页面基本结构)
3. [常用语法命令](#3-常用语法命令)
4. [数学公式](#4-数学公式)
5. [创建页面链接](#5-创建页面链接)
6. [常见错误与解决方案](#6-常见错误与解决方案)
7. [构建与预览](#7-构建与预览)

---

## 1. 创建新页面

### 1.1 确定页面编号

在 `trees/` 目录下，每个页面是一个以编号命名的 `.tree` 文件。

编号规则：
- 使用十六进制编号：`0001`, `0002`, `0003`, ..., `000A`, `000B`, `000C`, ..., `000F`, `0010`, ...
- 编号必须在整个站点中全局唯一，不能因为放进子目录就重复使用
- 可以放在子目录中（如 `math/algebra/`）来组织内容，但子目录不会自动成为 URL 命名空间

### 1.2 创建文件

在适当目录创建新文件，例如：

```bash
# 在根目录创建
touch trees/0021.tree

# 或在子目录创建
touch trees/math/algebra/0022.tree
```

### 1.3 最小模板

```tex
\title{页面标题}
\date{2025-03-25}
\taxon{类型}
\author{作者}

\p{这是页面内容。}
```

---

## 2. 页面基本结构

每个页面通常包含以下部分：

```tex
\title{页面标题}          % 必须：页面标题
\date{2025-03-25}         % 必须：日期
\taxon{Definition}        % 可选：类型标签
\author{作者}              % 可选：作者

% 下面是正文内容
\p{第一段文字。}

\strong{粗体标题}

\p{第二段文字。}
```

### 常用 taxon 类型

| taxon | 含义 |
|-------|------|
| `Definition` | 定义 |
| `Theorem` | 定理 |
| `Lemma` | 引理 |
| `Example` | 例子 |
| `Proof` | 证明 |
| `Chapter` | 章节 |
| `Introduction` | 介绍 |
| `Comparison` | 比较 |

---

## 3. 常用语法命令

### 3.1 段落

```tex
\p{这是普通段落文本。}
```

### 3.2 标题

```tex
\strong{粗体标题}
```

（注意：`\section` 和 `\subsection` 会产生警告，建议使用 `\strong` 作为替代）

### 3.3 无序列表

```tex
\ul{
  \li{第一项}
  \li{第二项}
  \li{第三项}
}
```

### 3.4 有序列表

```tex
\ol{
  \li{第一步}
  \li{第二步}
  \li{第三步}
}
```

### 3.5 嵌套列表

```tex
\ul{
  \li{第一项}
  \ol{
    \li{子项 1}
    \li{子项 2}
  }
  \li{第二项}
}
```

### 3.6 定义宏

```tex
\def\G#{\mathcal{G}}
\def\RR#{\mathbb{R}}

\p{设 #{$\mathbb{R}$} 表示实数集。}
\p{群 #{$\mathcal{G}$} 是一个集合。}
```

---

## 4. 数学公式

### 4.1 行内公式

使用 `#{$ ... $}` 语法：

```tex
\p{欧拉公式是 #{ $e^{i\pi} + 1 = 0$ }。}

\p{设 #{$\epsilon > 0$}，则存在 #{$\delta > 0$}。}
```

### 4.2 多行行内公式

```tex
\p{#{\mathbb{R}^n} 上的范数定义为
#{ $\|(x_1,\ldots,x_n)\| = \sqrt{|x_1|^2 + \cdots + |x_n|^2}$ }。}
```

### 4.3 常用符号

```tex
% 集合
#{ $\mathbb{N}$ }      % 自然数
#{ $\mathbb{Z}$ }      % 整数
#{ $\mathbb{R}$ }      % 实数
#{ $\mathbb{C}$ }      % 复数

% 字母
#{ $\mathcal{G}$ }     % 花体 G
#{ $\mathbb{R}$ }      % 黑板粗体 R
#{ $\ell^p$ }          % 序列空间

% 运算
#{ $x \in A$ }          % 属于
#{ $A \subseteq B$ }   % 子集
#{ $f: X \to Y$ }       % 映射
#{ $\sum_{i=1}^n$ }    % 求和
#{ $\int_a^b$ }        % 积分
```

### 4.4 矩阵和行列式

```tex
\p{矩阵 #{ $A = \begin{pmatrix} a & b \\ c & d \end{pmatrix}$ }。}
```

---

## 5. 创建页面链接

### 5.1 基本链接语法

```tex
[链接文本](页面编号)
```

示例：

```tex
\p{详见[群的基本定义](0001)。}
\p{更多信息请阅读[范畴论入门](0004)。}
```

### 5.2 链接到子目录中的页面

```tex
% 即使文件位于 math/topology/spaces/0010.tree
% 站内链接仍然使用全局 ID
[空集与单点集](0010)
```

### 5.3 带样式的链接

```tex
\li{\strong{[群的基本定义](0001)}：群的定义、结合律、单位元、逆元}
```

### 5.4 返回首页链接

```tex
\p{[返回博客首页](index)}
```

### 5.5 外部链接

```tex
\p{本博客使用 [Forester](https://www.forester-notes.org/) 构建。}
```

---

## 6. 常见错误与解决方案

### ❌ 禁止使用 `\[...\]` 显示数学公式

```tex
\[  \sum_{i=1}^n x_i  \]     % 错误
```

**正确写法：**

```tex
#{ $\sum_{i=1}^n x_i$ }
```

### ❌ 避免在 `#{}` 内使用圆括号 `)`

```tex
#{[a,b)}     % 错误
```

**正确写法：**

```tex
#{[a,b)}  #{a<b} 的区间
% 或
#{ $[a,b)$ }  表示左闭右开区间
```

### ❌ 避免使用 `\!` 等特殊 LaTeX 间距命令

```tex
#{(\!a, b)}     % 错误
```

**正确写法：**

```tex
#{(a,b)}
```

### ❌ 避免在 `#{}` 内嵌套花括号

```tex
#{\mathbb{R}^n}     % 可能出错
```

**正确写法：**

```tex
#{ $\mathbb{R}^n$ }
```

### ✅ 文件末尾保持换行

确保文件最后有一个空行。

### ✅ 使用 UTF-8 编码

所有文件必须保存为 UTF-8 编码。

---

## 7. 构建与预览

### 7.1 构建网站

```bash
# 进入项目目录
cd /Users/sunzy/claude_test/forester2

# 激活 OCaml 环境
eval $(opam env)

# 构建网站
forester build forest.toml
```

成功后会显示 `Success!`

### 7.2 本地预览

```bash
# 启动本地服务器
cd output
python3 -m http.server 8080

# 或从 output/math-blog 启动
cd output/math-blog
python3 -m http.server 8080
```

然后访问：
- 博客首页：http://localhost:8080/
- 或 http://localhost:8080/math-blog/

### 7.3 查看页面

页面 URL 格式：
- 根目录页面：`http://localhost:8080/math-blog/0001/`
- 子目录页面：`http://localhost:8080/math-blog/math/topology/0005/`

---

## 完整示例

以下是一个完整的页面示例：

```tex
\title{正交模格：量子逻辑的核心结构}
\date{2025-03-09}
\taxon{Definition}
\author{author}

\p{正交模格是量子逻辑中最重要的代数结构，它在量子力学中扮演着核心角色。}

\def\LL#{\mathcal{L}}

\strong{定义}

\p{一个\strong{正交模格} #{$\LL$} 是一个格，满足\strong{正交补}公理：对于所有 #{ $a, b \in \LL$}，若 #{ $a \perp b$}，则 #{ $a \vee b$} 存在且 #{ $a \vee b = (a \wedge b)^\perp$}。}

\strong{例子}

\ul{
  \li{布尔代数 #{$\mathcal{B}$} 是正交模格}
  \li{量子力学中的投影格 #{$\mathcal{P}(\mathcal{H})$} 是正交模格}
  \li{详见[正交模格的例子](0008)}
}

\strong{与经典逻辑的关系}

\p{正交模格是布尔代数的推广。在经典逻辑中，命题组成布尔代数；而在量子逻辑中，命题组成正交模格。详见[量子逻辑与经典逻辑的比较](000B)。}
```

---

## 快速参考卡

| 功能 | 语法 | 示例 |
|------|------|------|
| 标题 | `\title{}` | `\title{群的基本定义}` |
| 日期 | `\date{}` | `\date{2025-03-25}` |
| 类型 | `\taxon{}` | `\taxon{Definition}` |
| 段落 | `\p{}` | `\p{这是段落}` |
| 粗体标题 | `\strong{}` | `\strong{定义}` |
| 无序列表 | `\ul{ \li{} }` | `\ul{ \li{item} }` |
| 有序列表 | `\ol{ \li{} }` | `\ol{ \li{step} }` |
| 链接 | `[文本](URL)` | `[群](0001)` |
| 行内公式 | `#{$ ... $}` | `#{ $x^2$ }` |
| 定义宏 | `\def\X#{\mathcal{X}}` | `\def\G#{\mathcal{G}}` |

---

更新时间：2026-03-25
